function collect_ratings() {
    let ratings = {counts:0, sum:0, average: 0 };
    let rating = 0;
    document.querySelectorAll(".rating");
    const elements = [];
    elements.forEach (elememt => {
        element.replace("star", parseInt())
    })
   
    
}

